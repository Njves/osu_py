import cfg
class Map:
    def __init__(self, background, song, clap, bpm, colors, pace):
        self.background = background
        self.song = song
        self.clap = clap
        self.bpm = bpm
        self.colors = colors
        self.pace = pace